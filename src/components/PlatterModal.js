import React from "react";
import { Dialog } from "@mui/material";

const PlatterSelection = () => {
  return <Dialog>This is where the platter selection will go</Dialog>;
};

export default PlatterSelection;
